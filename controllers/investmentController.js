// services imports
// const {
//   investmentService: {
//     readInvestments,
//   }
// } = require('../services');

const investmentCreate = async (req, res) => {
  try {
    // controllers logics
  } catch (error) {
    console.error(error);
  }
};

module.exports = {
  investmentCreate,
};
